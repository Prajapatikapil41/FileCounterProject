{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "java",
      "name": "Run FileWordLineCounter",
      "request": "launch",
      "mainClass": "FileWordLineCounter",
      // ensure file paths like "data/input.txt" resolve correctly
      "cwd": "${workspaceFolder}"
    }
  ]
}